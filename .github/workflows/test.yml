name: GitHub Actions Demo

on: [push]

jobs:
  echo_hello_world:
    runs-on: ubuntu-latest
    list01:
      - true
    steps:
      - run: |
          echo "hello world!!"
          if [ true in list01 ];
            then echo "true"
          fi
