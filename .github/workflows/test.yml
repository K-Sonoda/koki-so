name: GitHub Actions Demo

on: [push]

jobs:
  echo_hello_world:
    runs-on: ubuntu-latest
    steps:
      - run: |
          if [ 'itg1' = 'itg1' ]; then 
            echo "true"
          else
            echo "false"
          fi
